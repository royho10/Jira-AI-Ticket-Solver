[pytest]
markers =
    deterministic: Fast, free checks (schema validation, keyword checks, no LLM calls)
    llm_eval: Requires real LLM calls (costs money, slower)
    integration: End-to-end pipeline tests (requires LLM + external services)

testpaths = tests
